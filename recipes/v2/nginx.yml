metadata:
  name: "NGINX"
  description: |
    NGINX is a self-hosted web application for web servers. Install it on your server for full control over your data and privacy.
  category: dev-infra
  subcategory: reverse-proxy
  tags: [Web Server, HTTP, Proxy]
  version: "latest"
  homepage: https://nginx.org/en/
  icon: https://www.google.com/s2/favicons?domain=nginx.org&sz=64
  license: Open Source
  verified: false
prerequisites:
  databases:
    postgres: false
    mysql: false
    redis: false
    mongodb: false
  ports: [80]
  resources:
    ram: 256
    disk: 1
install:
  docker:
    image: nginx:latest
    name: nginx
    port: "80:80"
    volumes:
      - /opt/srvly/apps/nginx/data:/data
    env: []
agent_install:
  network: srvly-nginx
  preflight:
    - "Check that the selected host port is free before deploying."
    - "Remove any failed nginx container before retrying."
  prerequisites: []
  app_env: {}
  post_install:
    - "Run the HTTP healthcheck before registering the installation as successful."
healthcheck:
  type: http
  port: 80
  path: /
  expected: [200, 301, 302]
  timeout: 60
params:
  image:
    type: string
    title: Docker image
    default: nginx:latest
    description: "Docker image to deploy"
  port:
    type: integer
    title: HTTP port
    default: 80
    description: "Host port for the web UI"
links:
  - label: Website
    url: https://nginx.org/en/
notes: |
  Auto-generated recipe. Verify ports and volumes before production use.
