metadata:
  name: "Mafl"
  description: |
    Mafl is a self-hosted application for personal dashboards. Install it on your server for full control over your data.
  category: dev-infra
  subcategory: server-management
  tags: [Dashboard, Startpage]
  version: "latest"
  homepage: https://mafl.hywax.space/
  icon: https://www.google.com/s2/favicons?domain=mafl.hywax.space&sz=64
  license: Open Source
  verified: false
prerequisites:
  databases:
    postgres: false
    mysql: false
    redis: false
    mongodb: false
  ports: [8080]
  resources:
    ram: 256
    disk: 1
install:
  docker:
    image: ghcr.io/hywax/mafl:latest
    name: mafl
    port: "8080:8080"
    volumes:
      - /opt/srvly/apps/mafl/data:/data
    env: []
agent_install:
  network: srvly-mafl
  preflight:
    - "Check that the selected host port is free before deploying."
    - "Remove any failed mafl container before retrying."
  prerequisites: []
  app_env: {}
  post_install:
    - "Run the HTTP healthcheck before registering the installation as successful."
healthcheck:
  type: http
  port: 8080
  path: /
  expected: [200, 301, 302]
  timeout: 60
params:
  image:
    type: string
    title: Docker image
    default: ghcr.io/hywax/mafl:latest
    description: "Docker image to deploy"
  port:
    type: integer
    title: HTTP port
    default: 8080
    description: "Host port for the web UI"
links:
  - label: Website
    url: https://mafl.hywax.space/
notes: |
  Auto-generated recipe. Verify ports and volumes before production use.
