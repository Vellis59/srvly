metadata:
  name: "Speedtest Tracker"
  description: |
    Speedtest Tracker is a self-hosted application for network utilities. Install it on your server for full control over your data.
  category: dev-infra
  subcategory: server-management
  tags: [Network, Utilities]
  version: "latest"
  homepage: https://docs.speedtest-tracker.dev/
  icon: https://www.google.com/s2/favicons?domain=docs.speedtest-tracker.dev&sz=64
  license: Open Source
  verified: false
prerequisites:
  databases:
    postgres: false
    mysql: false
    redis: false
    mongodb: false
  ports: [8080]
  resources:
    ram: 256
    disk: 1
install:
  docker:
    image: ghcr.io/alexjustesen/speedtest-tracker:latest
    name: speedtest-tracker
    port: "8080:8080"
    volumes:
      - /opt/srvly/apps/speedtest-tracker/data:/data
    env: []
agent_install:
  network: srvly-speedtest-tracker
  preflight:
    - "Check that the selected host port is free before deploying."
    - "Remove any failed speedtest-tracker container before retrying."
  prerequisites: []
  app_env: {}
  post_install:
    - "Run the HTTP healthcheck before registering the installation as successful."
healthcheck:
  type: http
  port: 8080
  path: /
  expected: [200, 301, 302]
  timeout: 60
params:
  image:
    type: string
    title: Docker image
    default: ghcr.io/alexjustesen/speedtest-tracker:latest
    description: "Docker image to deploy"
  port:
    type: integer
    title: HTTP port
    default: 8080
    description: "Host port for the web UI"
links:
  - label: Website
    url: https://docs.speedtest-tracker.dev/
notes: |
  Auto-generated recipe. Verify ports and volumes before production use.
