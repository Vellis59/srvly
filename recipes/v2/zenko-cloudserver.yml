metadata:
  name: "Zenko CloudServer"
  description: |
    Zenko CloudServer is a self-hosted application for file transfer - object storage & file servers. Install it on your server for full control over your data.
  category: storage-backup
  subcategory: file-sharing
  tags: [Files, Sharing]
  version: "latest"
  homepage: https://www.zenko.io/cloudserver
  icon: https://www.google.com/s2/favicons?domain=www.zenko.io&sz=64
  license: Open Source
  verified: false
prerequisites:
  databases:
    postgres: false
    mysql: false
    redis: false
    mongodb: false
  ports: [8080]
  resources:
    ram: 256
    disk: 1
install:
  docker:
    image: ghcr.io/scality/cloudserver:latest
    name: zenko-cloudserver
    port: "8080:8080"
    volumes:
      - /opt/srvly/apps/zenko-cloudserver/data:/data
    env: []
agent_install:
  network: srvly-zenko-cloudserver
  preflight:
    - "Check that the selected host port is free before deploying."
    - "Remove any failed zenko-cloudserver container before retrying."
  prerequisites: []
  app_env: {}
  post_install:
    - "Run the HTTP healthcheck before registering the installation as successful."
healthcheck:
  type: http
  port: 8080
  path: /
  expected: [200, 301, 302]
  timeout: 60
params:
  image:
    type: string
    title: Docker image
    default: ghcr.io/scality/cloudserver:latest
    description: "Docker image to deploy"
  port:
    type: integer
    title: HTTP port
    default: 8080
    description: "Host port for the web UI"
links:
  - label: Website
    url: https://www.zenko.io/cloudserver
notes: |
  Auto-generated recipe. Verify ports and volumes before production use.
