metadata:
  name: "MiroTalk C2C"
  description: |
    MiroTalk C2C is a self-hosted application for communication - video conferencing. Install it on your server for full control over your data.
  category: communication
  subcategory: chat
  tags: [Communication, Chat]
  version: "latest"
  homepage: https://c2c.mirotalk.com
  icon: https://www.google.com/s2/favicons?domain=c2c.mirotalk.com&sz=64
  license: Open Source
  verified: false
prerequisites:
  databases:
    postgres: false
    mysql: false
    redis: false
    mongodb: false
  ports: [8080]
  resources:
    ram: 256
    disk: 1
install:
  docker:
    image: ghcr.io/miroslavpejic85/mirotalkc2c:latest
    name: mirotalk-c2c
    port: "8080:8080"
    volumes:
      - /opt/srvly/apps/mirotalk-c2c/data:/data
    env: []
agent_install:
  network: srvly-mirotalk-c2c
  preflight:
    - "Check that the selected host port is free before deploying."
    - "Remove any failed mirotalk-c2c container before retrying."
  prerequisites: []
  app_env: {}
  post_install:
    - "Run the HTTP healthcheck before registering the installation as successful."
healthcheck:
  type: http
  port: 8080
  path: /
  expected: [200, 301, 302]
  timeout: 60
params:
  image:
    type: string
    title: Docker image
    default: ghcr.io/miroslavpejic85/mirotalkc2c:latest
    description: "Docker image to deploy"
  port:
    type: integer
    title: HTTP port
    default: 8080
    description: "Host port for the web UI"
links:
  - label: Website
    url: https://c2c.mirotalk.com
notes: |
  Auto-generated recipe. Verify ports and volumes before production use.
