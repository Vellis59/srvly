metadata:
  name: "Ownfoil"
  description: |
    Ownfoil is a self-hosted application for games - administrative utilities & control panels. Install it on your server for full control over your data.
  category: gaming
  subcategory: game-servers
  tags: [Gaming, Games]
  version: "latest"
  homepage: https://github.com/a1ex4/ownfoil
  icon: https://www.google.com/s2/favicons?domain=github.com&sz=64
  license: Open Source
  verified: false
prerequisites:
  databases:
    postgres: false
    mysql: false
    redis: false
    mongodb: false
  ports: [8080]
  resources:
    ram: 256
    disk: 1
install:
  docker:
    image: ghcr.io/a1ex4/ownfoil:latest
    name: ownfoil
    port: "8080:8080"
    volumes:
      - /opt/srvly/apps/ownfoil/data:/data
    env: []
agent_install:
  network: srvly-ownfoil
  preflight:
    - "Check that the selected host port is free before deploying."
    - "Remove any failed ownfoil container before retrying."
  prerequisites: []
  app_env: {}
  post_install:
    - "Run the HTTP healthcheck before registering the installation as successful."
healthcheck:
  type: http
  port: 8080
  path: /
  expected: [200, 301, 302]
  timeout: 60
params:
  image:
    type: string
    title: Docker image
    default: ghcr.io/a1ex4/ownfoil:latest
    description: "Docker image to deploy"
  port:
    type: integer
    title: HTTP port
    default: 8080
    description: "Host port for the web UI"
links:
  - label: Website
    url: https://github.com/a1ex4/ownfoil
notes: |
  Auto-generated recipe. Verify ports and volumes before production use.
