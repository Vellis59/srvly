metadata:
  name: "Apache HTTP Server"
  description: |
    The Apache HTTP Server is a secure, efficient and extensible web server providing HTTP services. The most popular web server since 1996.
  category: dev-infra
  subcategory: reverse-proxy
  tags: [Web Server, HTTP, Apache]
  version: "latest"
  homepage: https://httpd.apache.org/
  icon: https://www.google.com/s2/favicons?domain=httpd.apache.org&sz=64
  license: Apache-2.0
  verified: false
prerequisites:
  databases:
    postgres: false
    mysql: false
    redis: false
    mongodb: false
  ports: [80]
  resources:
    ram: 256
    disk: 1
install:
  docker:
    image: httpd:latest
    name: apachehttpserver
    port: "80:80"
    volumes:
      - /opt/srvly/apps/apachehttpserver/data:/data
    env: []
agent_install:
  network: srvly-apachehttpserver
  preflight:
    - "Check that the selected host port is free before deploying."
    - "Remove any failed apachehttpserver container before retrying."
  prerequisites: []
  app_env: {}
  post_install:
    - "Run the HTTP healthcheck before registering the installation as successful."
healthcheck:
  type: http
  port: 80
  path: /
  expected: [200, 301, 302]
  timeout: 60
params:
  image:
    type: string
    title: Docker image
    default: httpd:latest
    description: "Docker image to deploy"
  port:
    type: integer
    title: HTTP port
    default: 80
    description: "Host port for the web UI"
links:
  - label: Website
    url: https://httpd.apache.org/
notes: |
  Auto-generated recipe. Verify ports and volumes before production use.
