metadata:
  name: "Local Content Share"
  description: |
    Local Content Share is a self-hosted application for pastebins. Install it on your server for full control over your data.
  category: productivity
  subcategory: notes-wiki
  tags: [Pastebin, Code, Sharing]
  version: "latest"
  homepage: https://github.com/Tanq16/local-content-share
  icon: https://www.google.com/s2/favicons?domain=github.com&sz=64
  license: Open Source
  verified: false
prerequisites:
  databases:
    postgres: false
    mysql: false
    redis: false
    mongodb: false
  ports: [8080]
  resources:
    ram: 256
    disk: 1
install:
  docker:
    image: ghcr.io/tanq16/local-content-share:latest
    name: local-content-share
    port: "8080:8080"
    volumes:
      - /opt/srvly/apps/local-content-share/data:/data
    env: []
agent_install:
  network: srvly-local-content-share
  preflight:
    - "Check that the selected host port is free before deploying."
    - "Remove any failed local-content-share container before retrying."
  prerequisites: []
  app_env: {}
  post_install:
    - "Run the HTTP healthcheck before registering the installation as successful."
healthcheck:
  type: http
  port: 8080
  path: /
  expected: [200, 301, 302]
  timeout: 60
params:
  image:
    type: string
    title: Docker image
    default: ghcr.io/tanq16/local-content-share:latest
    description: "Docker image to deploy"
  port:
    type: integer
    title: HTTP port
    default: 8080
    description: "Host port for the web UI"
links:
  - label: Website
    url: https://github.com/Tanq16/local-content-share
notes: |
  Auto-generated recipe. Verify ports and volumes before production use.
