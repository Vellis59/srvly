metadata:
  name: "Typebot"
  description: |
    Typebot is a self-hosted application for communication - custom communication systems. Install it on your server for full control over your data.
  category: communication
  subcategory: chat
  tags: [Communication, Chat]
  version: "latest"
  homepage: https://typebot.io
  icon: https://www.google.com/s2/favicons?domain=typebot.io&sz=64
  license: Open Source
  verified: false
prerequisites:
  databases:
    postgres: false
    mysql: false
    redis: false
    mongodb: false
  ports: [8080]
  resources:
    ram: 256
    disk: 1
install:
  docker:
    image: ghcr.io/baptistearno/typebot:latest
    name: typebot
    port: "8080:8080"
    volumes:
      - /opt/srvly/apps/typebot/data:/data
    env: []
agent_install:
  network: srvly-typebot
  preflight:
    - "Check that the selected host port is free before deploying."
    - "Remove any failed typebot container before retrying."
  prerequisites: []
  app_env: {}
  post_install:
    - "Run the HTTP healthcheck before registering the installation as successful."
healthcheck:
  type: http
  port: 8080
  path: /
  expected: [200, 301, 302]
  timeout: 60
params:
  image:
    type: string
    title: Docker image
    default: ghcr.io/baptistearno/typebot:latest
    description: "Docker image to deploy"
  port:
    type: integer
    title: HTTP port
    default: 8080
    description: "Host port for the web UI"
links:
  - label: Website
    url: https://typebot.io
notes: |
  Auto-generated recipe. Verify ports and volumes before production use.
