metadata:
  name: "AFFiNE Community Edition"
  description: |
    AFFiNE Community Edition is a self-hosted application for knowledge management tools. Install it on your server for full control over your data.
  category: productivity
  subcategory: notes-wiki
  tags: [Knowledge, Wiki]
  version: "latest"
  homepage: https://affine.pro/
  icon: https://www.google.com/s2/favicons?domain=affine.pro&sz=64
  license: Open Source
  verified: false
prerequisites:
  databases:
    postgres: false
    mysql: false
    redis: false
    mongodb: false
  ports: [8080]
  resources:
    ram: 256
    disk: 1
install:
  docker:
    image: ghcr.io/toeverything/affine:latest
    name: affine-community-edition
    port: "8080:8080"
    volumes:
      - /opt/srvly/apps/affine-community-edition/data:/data
    env: []
agent_install:
  network: srvly-affine-community-edition
  preflight:
    - "Check that the selected host port is free before deploying."
    - "Remove any failed affine-community-edition container before retrying."
  prerequisites: []
  app_env: {}
  post_install:
    - "Run the HTTP healthcheck before registering the installation as successful."
healthcheck:
  type: http
  port: 8080
  path: /
  expected: [200, 301, 302]
  timeout: 60
params:
  image:
    type: string
    title: Docker image
    default: ghcr.io/toeverything/affine:latest
    description: "Docker image to deploy"
  port:
    type: integer
    title: HTTP port
    default: 8080
    description: "Host port for the web UI"
links:
  - label: Website
    url: https://affine.pro/
notes: |
  Auto-generated recipe. Verify ports and volumes before production use.
