metadata:
  name: "Static Web Server"
  description: |
    Static Web Server is a self-hosted application for web servers. Install it on your server for full control over your data.
  category: dev-infra
  subcategory: reverse-proxy
  tags: [Web Server, HTTP, Proxy]
  version: "latest"
  homepage: https://static-web-server.net/
  icon: https://www.google.com/s2/favicons?domain=static-web-server.net&sz=64
  license: Open Source
  verified: false
prerequisites:
  databases:
    postgres: false
    mysql: false
    redis: false
    mongodb: false
  ports: [80]
  resources:
    ram: 256
    disk: 1
install:
  docker:
    image: ghcr.io/static-web-server/static-web-server:latest
    name: static-web-server
    port: "80:80"
    volumes:
      - /opt/srvly/apps/static-web-server/data:/data
    env: []
agent_install:
  network: srvly-static-web-server
  preflight:
    - "Check that the selected host port is free before deploying."
    - "Remove any failed static-web-server container before retrying."
  prerequisites: []
  app_env: {}
  post_install:
    - "Run the HTTP healthcheck before registering the installation as successful."
healthcheck:
  type: http
  port: 80
  path: /
  expected: [200, 301, 302]
  timeout: 60
params:
  image:
    type: string
    title: Docker image
    default: ghcr.io/static-web-server/static-web-server:latest
    description: "Docker image to deploy"
  port:
    type: integer
    title: HTTP port
    default: 80
    description: "Host port for the web UI"
links:
  - label: Website
    url: https://static-web-server.net/
notes: |
  Auto-generated recipe. Verify ports and volumes before production use.
