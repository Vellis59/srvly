metadata:
  name: "Domain Locker"
  description: |
    Domain Locker is a self-hosted application for miscellaneous. Install it on your server for full control over your data.
  category: other
  subcategory: 
  tags: [Other, Miscellaneous]
  version: "latest"
  homepage: https://domain-locker.com
  icon: https://www.google.com/s2/favicons?domain=domain-locker.com&sz=64
  license: Open Source
  verified: false
prerequisites:
  databases:
    postgres: false
    mysql: false
    redis: false
    mongodb: false
  ports: [8080]
  resources:
    ram: 256
    disk: 1
install:
  docker:
    image: ghcr.io/lissy93/domain-locker:latest
    name: domain-locker
    port: "8080:8080"
    volumes:
      - /opt/srvly/apps/domain-locker/data:/data
    env: []
agent_install:
  network: srvly-domain-locker
  preflight:
    - "Check that the selected host port is free before deploying."
    - "Remove any failed domain-locker container before retrying."
  prerequisites: []
  app_env: {}
  post_install:
    - "Run the HTTP healthcheck before registering the installation as successful."
healthcheck:
  type: http
  port: 8080
  path: /
  expected: [200, 301, 302]
  timeout: 60
params:
  image:
    type: string
    title: Docker image
    default: ghcr.io/lissy93/domain-locker:latest
    description: "Docker image to deploy"
  port:
    type: integer
    title: HTTP port
    default: 8080
    description: "Host port for the web UI"
links:
  - label: Website
    url: https://domain-locker.com
notes: |
  Auto-generated recipe. Verify ports and volumes before production use.
