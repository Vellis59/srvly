metadata:
  name: "imgproxy"
  description: |
    imgproxy is a self-hosted application for proxy. Install it on your server for full control over your data.
  category: dev-infra
  subcategory: reverse-proxy
  tags: [Proxy, Reverse, Network]
  version: "latest"
  homepage: https://imgproxy.net/
  icon: https://www.google.com/s2/favicons?domain=imgproxy.net&sz=64
  license: Open Source
  verified: false
prerequisites:
  databases:
    postgres: false
    mysql: false
    redis: false
    mongodb: false
  ports: [80]
  resources:
    ram: 256
    disk: 1
install:
  docker:
    image: ghcr.io/imgproxy/imgproxy:latest
    name: imgproxy
    port: "80:80"
    volumes:
      - /opt/srvly/apps/imgproxy/data:/data
    env: []
agent_install:
  network: srvly-imgproxy
  preflight:
    - "Check that the selected host port is free before deploying."
    - "Remove any failed imgproxy container before retrying."
  prerequisites: []
  app_env: {}
  post_install:
    - "Run the HTTP healthcheck before registering the installation as successful."
healthcheck:
  type: http
  port: 80
  path: /
  expected: [200, 301, 302]
  timeout: 60
params:
  image:
    type: string
    title: Docker image
    default: ghcr.io/imgproxy/imgproxy:latest
    description: "Docker image to deploy"
  port:
    type: integer
    title: HTTP port
    default: 80
    description: "Host port for the web UI"
links:
  - label: Website
    url: https://imgproxy.net/
notes: |
  Auto-generated recipe. Verify ports and volumes before production use.
