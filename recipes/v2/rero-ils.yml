metadata:
  name: "RERO ILS"
  description: |
    RERO ILS is a self-hosted application for document management - integrated library systems (ils). Install it on your server for full control over your data.
  category: productivity
  subcategory: documents
  tags: [Documents, PDF]
  version: "latest"
  homepage: https://rero21.ch/
  icon: https://www.google.com/s2/favicons?domain=rero21.ch&sz=64
  license: Open Source
  verified: false
prerequisites:
  databases:
    postgres: false
    mysql: false
    redis: false
    mongodb: false
  ports: [8080]
  resources:
    ram: 256
    disk: 1
install:
  docker:
    image: ghcr.io/rero/rero-ils:latest
    name: rero-ils
    port: "8080:8080"
    volumes:
      - /opt/srvly/apps/rero-ils/data:/data
    env: []
agent_install:
  network: srvly-rero-ils
  preflight:
    - "Check that the selected host port is free before deploying."
    - "Remove any failed rero-ils container before retrying."
  prerequisites: []
  app_env: {}
  post_install:
    - "Run the HTTP healthcheck before registering the installation as successful."
healthcheck:
  type: http
  port: 8080
  path: /
  expected: [200, 301, 302]
  timeout: 60
params:
  image:
    type: string
    title: Docker image
    default: ghcr.io/rero/rero-ils:latest
    description: "Docker image to deploy"
  port:
    type: integer
    title: HTTP port
    default: 8080
    description: "Host port for the web UI"
links:
  - label: Website
    url: https://rero21.ch/
notes: |
  Auto-generated recipe. Verify ports and volumes before production use.
