metadata:
  name: "Judge0 CE"
  description: |
    Judge0 CE is a self-hosted application for software development - ide & tools. Install it on your server for full control over your data.
  category: dev-infra
  subcategory: git-code
  tags: [Dev, Git, Code]
  version: "latest"
  homepage: https://judge0.com
  icon: https://www.google.com/s2/favicons?domain=judge0.com&sz=64
  license: Open Source
  verified: false
prerequisites:
  databases:
    postgres: false
    mysql: false
    redis: false
    mongodb: false
  ports: [8080]
  resources:
    ram: 256
    disk: 1
install:
  docker:
    image: ghcr.io/judge0/judge0:latest
    name: judge0-ce
    port: "8080:8080"
    volumes:
      - /opt/srvly/apps/judge0-ce/data:/data
    env: []
agent_install:
  network: srvly-judge0-ce
  preflight:
    - "Check that the selected host port is free before deploying."
    - "Remove any failed judge0-ce container before retrying."
  prerequisites: []
  app_env: {}
  post_install:
    - "Run the HTTP healthcheck before registering the installation as successful."
healthcheck:
  type: http
  port: 8080
  path: /
  expected: [200, 301, 302]
  timeout: 60
params:
  image:
    type: string
    title: Docker image
    default: ghcr.io/judge0/judge0:latest
    description: "Docker image to deploy"
  port:
    type: integer
    title: HTTP port
    default: 8080
    description: "Host port for the web UI"
links:
  - label: Website
    url: https://judge0.com
notes: |
  Auto-generated recipe. Verify ports and volumes before production use.
