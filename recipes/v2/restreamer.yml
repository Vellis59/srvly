metadata:
  name: "Restreamer"
  description: |
    Restreamer is a self-hosted application for media streaming - video streaming. Install it on your server for full control over your data.
  category: media
  subcategory: media-management
  tags: [Media, Streaming]
  version: "latest"
  homepage: https://datarhei.github.io/restreamer/
  icon: https://www.google.com/s2/favicons?domain=datarhei.github.io&sz=64
  license: Open Source
  verified: false
prerequisites:
  databases:
    postgres: false
    mysql: false
    redis: false
    mongodb: false
  ports: [8096]
  resources:
    ram: 256
    disk: 1
install:
  docker:
    image: ghcr.io/datarhei/restreamer:latest
    name: restreamer
    port: "8096:8096"
    volumes:
      - /opt/srvly/apps/restreamer/data:/data
    env: []
agent_install:
  network: srvly-restreamer
  preflight:
    - "Check that the selected host port is free before deploying."
    - "Remove any failed restreamer container before retrying."
  prerequisites: []
  app_env: {}
  post_install:
    - "Run the HTTP healthcheck before registering the installation as successful."
healthcheck:
  type: http
  port: 8096
  path: /
  expected: [200, 301, 302]
  timeout: 60
params:
  image:
    type: string
    title: Docker image
    default: ghcr.io/datarhei/restreamer:latest
    description: "Docker image to deploy"
  port:
    type: integer
    title: HTTP port
    default: 8096
    description: "Host port for the web UI"
links:
  - label: Website
    url: https://datarhei.github.io/restreamer/
notes: |
  Auto-generated recipe. Verify ports and volumes before production use.
