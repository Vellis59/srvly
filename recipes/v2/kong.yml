metadata:
  name: "Kong"
  description: |
    Kong is a self-hosted application for software development - api management. Install it on your server for full control over your data.
  category: dev-infra
  subcategory: git-code
  tags: [Dev, Git, Code]
  version: "latest"
  homepage: https://konghq.com/kong/
  icon: https://www.google.com/s2/favicons?domain=konghq.com&sz=64
  license: Open Source
  verified: false
prerequisites:
  databases:
    postgres: false
    mysql: false
    redis: false
    mongodb: false
  ports: [8080]
  resources:
    ram: 256
    disk: 1
install:
  docker:
    image: ghcr.io/kong/kong:latest
    name: kong
    port: "8080:8080"
    volumes:
      - /opt/srvly/apps/kong/data:/data
    env: []
agent_install:
  network: srvly-kong
  preflight:
    - "Check that the selected host port is free before deploying."
    - "Remove any failed kong container before retrying."
  prerequisites: []
  app_env: {}
  post_install:
    - "Run the HTTP healthcheck before registering the installation as successful."
healthcheck:
  type: http
  port: 8080
  path: /
  expected: [200, 301, 302]
  timeout: 60
params:
  image:
    type: string
    title: Docker image
    default: ghcr.io/kong/kong:latest
    description: "Docker image to deploy"
  port:
    type: integer
    title: HTTP port
    default: 8080
    description: "Host port for the web UI"
links:
  - label: Website
    url: https://konghq.com/kong/
notes: |
  Auto-generated recipe. Verify ports and volumes before production use.
